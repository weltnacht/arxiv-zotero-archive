interface:
  display_name: "arXiv Zotero Archive"
  short_description: "Search arXiv by topic and archive preprints into Zotero."
  default_prompt: "Use $arxiv-zotero-archive to search arXiv for recent papers on a keyword, export RIS/BibTeX plus a concise English table by default (or Chinese if requested), and archive them into a Zotero collection."
